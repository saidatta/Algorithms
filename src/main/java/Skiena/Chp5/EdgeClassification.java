package Skiena.Chp5;

/**
 * Created by venkatamunnangi on 1/2/17.
 */
public class EdgeClassification {
    int edgeClassification(int x ,int y) { // x -> y
        // if(parent[y] == x) - Tree Edge
        // !disovered(y) && disovered(x) - Forward Edge
        // anscestor(y) == x && discovered(x) - back edge
        // ancestor(y) != x - cross edge
        return 0;
    }
}
